// upgrades.js for bosch-hd-cameracontrol

// No upgrade scripts needed yet.
// Add functions here in the future if config, actions, or feedbacks change.
// See https://github.com/bitfocus/companion/wiki/Module-Development-Upgrades

module.exports = [
	// Example upgrade function structure:
	/*
	function(context, props) {
		const result = {
			updatedConfig: null,
			updatedActions: [],
			updatedFeedbacks: [],
		}

		// Check props.config, props.actions, props.feedbacks
		// and modify them, adding updated ones to the result object.

		return result
	},
	*/
]